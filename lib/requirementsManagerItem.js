'use babel';

// Define the model of the Requirements Manager
export default class RequirementsManagerItem {
  /**
   * @description The title for the workspace's pane
   * @return {string}
   * The pane's title
   */
  getTitle() {
    return 'Requirements Manager';
  }
}
